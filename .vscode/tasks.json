{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "jekyll:serve:watch",
      "type": "shell",
      "command": "bundle",
      "args": ["exec", "jekyll", "serve", "--watch", "-P", "4001"],
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    },
    {
      "label": "jekyll:serve:livereload",
      "type": "shell",
      "command": "bundle",
      "args": ["exec", "jekyll", "serve", "--watch", "--livereload", "-P", "4002"],
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    }
  ]
}
